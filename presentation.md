# Next.js the futur of React development

## Main topics to cover

* Zero config starting point
    * Then easy webpack override/composition with plugins
* Automatic code splitting with `./pages` folder
* Static files in `./static`
* Smart Server Side Rendering
    * `<Link />` component
    * Component `getInitialProps()` method
* Dev server with hot reload
* Easy build and static export
